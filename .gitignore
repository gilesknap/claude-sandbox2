# Editor / OS
.DS_Store
*.swp

# Claude local-only settings (per-user, not committed)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# vscode workspaces
*.code-workspace
