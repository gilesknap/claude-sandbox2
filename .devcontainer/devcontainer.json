// For format details, see https://containers.dev/implementors/json_reference/
{
    "name": "claude-sandbox dev container",
    "build": {
        "dockerfile": "../Dockerfile",
        "target": "developer"
    },
    "remoteEnv": {
        // Allow X11 apps to run inside the container.
        "DISPLAY": "${localEnv:DISPLAY}",
        "IN_DEVCONTAINER": "1"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "github.vscode-github-actions",
                "ms-azuretools.vscode-docker"
            ]
        }
    },
    "runArgs": [
        // Allow the container to access the host X11 display and EPICS CA.
        "--net=host",
        // Make sure SELinux does not disable host filesystems like tmp.
        "--security-opt=label=disable"
    ],
    "mounts": [
        // Single shared-config bind. install.sh's link_terminal_config
        // step symlinks /root/.claude{,.json} into here, so dogfood and
        // consumer devcontainers go through the exact same path.
        {
            "source": "${localEnv:HOME}/.config/terminal-config",
            "target": "/user-terminal-config",
            "type": "bind"
        }
    ],
    // Mount the parent as /workspaces so peer clones are reachable.
    "workspaceMount": "source=${localWorkspaceFolder}/..,target=/workspaces,type=bind",
    "postCreateCommand": ".devcontainer/postCreate.sh",
    "initializeCommand": ".devcontainer/initializeCommand.sh"
}
